export type UrlType = {
  url: string;
  type: string;
  alias: string, 
  shortId: string, 
  originalUrl: string, 
  created_at: string, 
  expiration: string, 
  passwordHash: string, 
  clicks: number, 
  id: string
}

